namespace = botb_struggle

botb_struggle.1001 = {
    type = character_event
    window = fullscreen_event
    title = botb_struggle.0001.t
    desc = botb_struggle.0001.desc
    theme = faith
    override_background = {reference = botb_throneroom_fullscreen}

   


    option = { 
        name = botb_struggle.1001.a
        add_prestige = 2000

        ai_chance = {
            base = 100
        }
    }
	after = {

        baltic_struggle_ending_involved_list_effect = yes
        every_player = {
            limit = {
                does_this_player_care_about_baltic = yes
            }
            add_to_list = players_to_notify
        }
        struggle:baltic_struggle = {
            end_struggle = baltic_struggle_new_ways_decision_ending
        }
        every_in_list = {
            list = players_to_notify
            trigger_event = botb_struggle.1011
        }
	}
}

botb_struggle.1011 = {
    type = character_event
	window = fullscreen_event
	title = botb_struggle.1011.t
	desc = {
		first_valid = {
		    triggered_desc = {
		        trigger = { is_in_list = struggle_involvees }
		        desc = botb_struggle.1011.opening.involved
		    }
		    desc = botb_struggle.1011.opening
		}
		desc = botb_struggle.1011.desc
        desc = botb_struggle.1011.closing.combined

	}
	theme = court
	override_background = { reference = botb_throneroom_fullscreen }

	# Ok
	option = {
		name = {
			text = botb_struggle.1011.a
			trigger = { is_in_list = struggle_involvees }
		}
		name = {
			text = botb_struggle.1011.b
			trigger = {
				NOT = { is_in_list = struggle_involvees }
			}
		}
	}
}