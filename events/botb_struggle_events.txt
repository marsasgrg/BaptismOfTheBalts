namespace = baltic_struggle

baltic_struggle.0501 = {
	type = character_event
	window = fullscreen_event
	title = baltic_struggle.0501.t
	desc = {
		desc = baltic_struggle.0501.desc
		first_valid = {
			triggered_desc = {
				trigger = {
					any_character_struggle = {
						involvement = involved
						is_struggle_type = baltic_struggle
					}
				}
				desc = baltic_struggle.0501.desc.involved
			}
			triggered_desc = {
				trigger = {
					any_character_struggle = {
						involvement = interloper
						is_struggle_type = baltic_struggle
					}
				}
				desc = baltic_struggle.0501.desc.interloper
			}
		}
	}
	theme = realm
	trigger = { # we need to prevent this from firing for all players when a new player joins in MP
		is_ai = no
		NOT = { has_character_flag = baltic_struggle_intro_event_flag }
		any_character_struggle = {
			is_struggle_type = baltic_struggle
		}
 	}
	override_background = { reference = botb_forest_fullscreen }

	cooldown = { years = 100 }
	immediate = {
		
		add_character_flag = baltic_struggle_intro_event_flag
	}
	widgets = {
		widget = {
 			gui = "event_window_widget_struggle_info"
 			container = "dynamic_content_widget"
 			controller = struggle_info
 			setup_scope = { struggle:baltic_struggle = { save_scope_as = struggle } }
		}
	}
	option = {
		name = baltic_struggle.0501.a
	}
}

baltic_struggle.1001 = {
    type = character_event
    window = fullscreen_event
    title = baltic_struggle.1001.t
    desc = baltic_struggle.1001.desc
    
    theme = faith
    override_background = {reference = botb_throneroom_fullscreen}

   


    option = { 
        name = baltic_struggle.1001.a
        add_prestige = 2000

        ai_chance = {
            base = 100
        }
    }
	after = {

        baltic_struggle_ending_involved_list_effect = yes
        every_player = {
            limit = {
                does_this_player_care_about_baltic = yes
            }
            add_to_list = players_to_notify
        }
        struggle:baltic_struggle = {
            end_struggle = baltic_struggle_new_ways_decision_ending
        }
        every_in_list = {
            list = players_to_notify
            trigger_event = baltic_struggle.1011
        }
	}
}
baltic_struggle.1002 = {
    type = character_event
    window = fullscreen_event
    title = baltic_struggle.1002.t
    desc = baltic_struggle.1002.desc
    
    theme = faith
    override_background = {reference = botb_throneroom_fullscreen}

   


    option = { 
        name = baltic_struggle.1002.a
        add_prestige = 2000

        ai_chance = {
            base = 100
        }
    }
	after = {

        baltic_struggle_ending_involved_list_effect = yes
        every_player = {
            limit = {
                does_this_player_care_about_baltic = yes
            }
            add_to_list = players_to_notify
        }
        struggle:baltic_struggle = {
            end_struggle = baltic_struggle_old_ways_decision_ending
        }
        every_in_list = {
            list = players_to_notify
            trigger_event = baltic_struggle.1011
        }
	}
}

baltic_struggle.1003 = {
    type = character_event
    window = fullscreen_event
    title = baltic_struggle.1003.t
    desc = baltic_struggle.1003.desc
    
    theme = faith
    override_background = {reference = botb_devastation_fullscreen}

   


    option = { 
        name = baltic_struggle.1003.a
        add_prestige = 2000

        ai_chance = {
            base = 100
        }
    }
	after = {

        baltic_struggle_ending_involved_list_effect = yes
        every_player = {
            limit = {
                does_this_player_care_about_baltic = yes
            }
            add_to_list = players_to_notify
        }
        struggle:baltic_struggle = {
            end_struggle = baltic_struggle_colonial_decision_ending
        }
        every_in_list = {
            list = players_to_notify
            trigger_event = baltic_struggle.1013
        }
	}
}



baltic_struggle.1011 = {
    type = character_event
	window = fullscreen_event
	title = baltic_struggle.1011.t
	desc = {
		first_valid = {
		    triggered_desc = {
		        trigger = { is_in_list = struggle_involvees }
		        desc = baltic_struggle.1011.opening.involved
		    }
		    desc = baltic_struggle.1011.opening
		}
		desc = baltic_struggle.1011.desc
        desc = baltic_struggle.1011.closing.combined

	}
	theme = court
	override_background = { reference = botb_throneroom_fullscreen }

	# Ok
	option = {
		name = {
			text = baltic_struggle.1011.a
			trigger = { is_in_list = struggle_involvees }
		}
		name = {
			text = baltic_struggle.1011.b
			trigger = {
				NOT = { is_in_list = struggle_involvees }
			}
		}
	}
}

baltic_struggle.1013 = {
    type = character_event
	window = fullscreen_event
	title = baltic_struggle.1013.t
	desc = {
		first_valid = {
		    triggered_desc = {
		        trigger = { is_in_list = struggle_involvees }
		        desc = baltic_struggle.1013.opening.involved
		    }
		    desc = baltic_struggle.1013.opening
		}
		desc = baltic_struggle.1013.desc
        desc = baltic_struggle.1013.closing.combined

	}
	theme = court
	override_background = { reference = botb_devastation_fullscreen }

	# Ok
	option = {
		name = {
			text = baltic_struggle.1013.a
			trigger = { 
				culture = scope:struggle_ender.culture
			}
		}
		name = {
			text = baltic_struggle.1013.b
			trigger = {
				NOT = { 
					culture = scope:struggle_ender.culture
				}
			}
		}
	}
}

